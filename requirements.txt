# --- Core data + modeling ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# --- Web API ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# --- AI assistant (RAG + open-source LLM) ---
sentence-transformers>=2.6   # text embeddings (open source)
transformers>=4.40           # runs the open-source LLM (FLAN-T5)
torch>=2.0                   # backend for transformers

# --- Testing ---
pytest>=8.0
httpx>=0.27                  # used by FastAPI's TestClient
